{
  "name": "nuevue-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "NueVue monorepo — AI-integrated user nexus platform",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
